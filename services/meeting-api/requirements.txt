fastapi>=0.128.8
uvicorn[standard]>=0.39.0
httpx>=0.28.1
h11>=0.16.0                       # CVE-2025-43859
idna>=3.15
redis>=5.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic[email]>=2.4.0
alembic>=1.13
python-multipart>=0.0.20
boto3>=1.34
# YouTube transcript endpoint (meeting_api/youtube_transcribe.py). Invoked as a
# subprocess, not imported — YouTube breaks extractors often, and a wedged one
# must not be able to take meeting-api down. Pin loosely and update often: an
# out-of-date yt-dlp is the usual cause of "Video unavailable" on valid links.
# ffmpeg (already in the Dockerfile) does the audio extract on the ASR path.
yt-dlp>=2026.7.4

Mako>=1.3.12
